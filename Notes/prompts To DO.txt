
todo {{

in this func backend_pipeline.data_pipeline.populator_scripts.DBPopulateEntityEnrichment.DBPopulateEntityEnrichment._get_unenriched_entities_for_source

we retrieve Entitys. 
but, even though in my DB, the entity with key X has indeed metadata, the entity obj that my code pulls from the DB does NOT has metadata. for example - it does not have Person does not have Person.display_heb_name populated. why not?

backend.db.mongo_parts.entity_mixin.EntityMongoMixin.get_entities_by_keys
backend.models_db.EntityObjects.EPerson.EPerson

}}



#################################################################################
########################## populate metadata ############################
#################################################################################

finished - entity enrichemnt add to db {{ 

see backend_pipeline.data_pipeline.populator_scripts.DBPopulateEntityEnrichment.DBPopulateEntityEnrichment._extract_all_to_json

here - json_str, usage, cost_usd = await self._extract_from_passage(passage, entity_json_list) we have the json string of entities. it can look like this:
{
    "entities": [
        {
            "key": "6a5fa5fe011460404dfc3eea",
            "display_heb_name": "אלהים",
            "timePeriod": "Tanach"
        },
        {
            "key": "6a5fa694011460404dfc4176",
            "display_heb_name": "רחל",
            "timePeriod": "Tanach",
            "isWoman": true
        },
 
    ],
    "key": "TN_Genesis_0_30:3-8"
}

now, i want you to reinsert those entities into the DB. 
(1) make sure to verify the Enum values with the actual values.
(2) notice - there can be fields missing
(3) we already have this func - backend.db.mongo_parts.entity_mixin.EntityMongoMixin.update_entity - just make sure that it makes sense. 
 }}
 
FINISHED impl populate entity metadata {{

----
implement: backend_pipeline.data_pipeline.populator_scripts.DBPopulateLlmBase.DBPopulateLlmBase._extract_from_passage 

for 
backend_pipeline.data_pipeline.populator_scripts.DBPopulateEntityEnrichment.DBPopulateEntityEnrichment

see how we did it in backend_pipeline.data_pipeline.populator_scripts.DBPopulateEntityRelGraph.DBPopulateLmmData

but this time:
the 'passage' will include both the clean english, and clean hebrew text (no vowels), and in addition to the package, we will send a list of entities (in json format) that we will get from the DB. DONT WORRY ABOUT:
- the runner code/loop
- which entities we'll eventually get

JUST FOCUS ON THE PYDANTIC CALLING CODE.

We must add an optional param to this func, the func should be a list of Strings 'entity_json_list'

we will be sending the passage (eng + heb), and a list of Entitys associated with that Passage (derived from Source Metadata) that have not yet been enriched with metadata (if an entity has already been enriched, it will not be sent). 


how the prompt to pydantic should look:
We want pydantic to essentially fill in the fields that the db didnt. so:
for every Entity: display_heb_name. This should be taken almost exactly from the hebrew text sent in passage in prompt, but should be normalized. no vowels.
Person: timePeriod (send backend.models_db.Enums.TimePeriod enum), isWoman (bool), isNonJew (bool), isGroup (bool), roles (send backend.models_db.Enums.RoleType)
Number: heb_unit, heb_context. No Vowels, same direction as english unit and context.
Place: PlaceType (send enum backend.models_db.Enums.PlaceType)
Symbol: SymbolType (send enum backend.models_db.Enums.SymbolType)

validate - if a value received for Person.timePeriod, Person.roleType, place.placeType, symbol.SymbolType does not exist in the original enum - then simply filter it out of the final json response (log it, like that way we long in populate Entity Rel Graph)

-------
 implement
backend_pipeline/data_pipeline/populator_scripts/DBPopulateEntityEnrichment.py  : 

i will describe everything which must happen, some of the code might already be written in base files / pydanticCaller, etc. 
if doesnt exist there, you will probably find similar code in backend_pipeline.data_pipeline.populator_scripts.DBPopulateEntityRelGraph.DBPopulateLmmData

1. iterate over sources, for now from get_examples_src_contents
2. for every source, query the db and get the list of Entitys linked to that source. 
3. if an Entity already has metadata - remove it from the list (ie we dont want to touch it). use Entity.has_metadata

4. send to the pydantic caller both the clean english and clean hebrew text, with entity list.
5. just like we do in DBPopulateEntityRelGraph, print the output as txt files nad jsons.. in a nice dir. if we dont already have a good dir, feel free to create one.


}}

Merge/Join Entitys s {{

heres what an entity looks in our db:

{
  "_id": {
    "$oid": "6a467c2701fbb1a2983bf4fb"
  },
  "display_en_name": "god",
  "display_heb_name": "",
  "all_en_names": [
    "God"
  ],
  "all_heb_names": [],
  "entityType": "Person",
  "alias_keys": [],
  "key": "6a467c2701fbb1a2983bf4fb"
}

or:

{
  "_id": {
    "$oid": "6a467c2701fbb1a2983bf4fc"
  },
  "display_en_name": "heaven",
  "display_heb_name": "",
  "all_en_names": [
    "Heaven"
  ],
  "all_heb_names": [],
  "entityType": "Place",
  "alias_keys": [],
  "key": "6a467c2701fbb1a2983bf4fc"
}


display_en_name is usually unique

look at relationship:
{
  "_id": {
    "$oid": "6a467d1801fbb1a2983bf93f"
  },
  "term1": "6a467c2701fbb1a2983bf4fb",
  "term2": "6a467c2701fbb1a2983bf4fc",
  "rel_type": "associatedWithPlace",
  "key": "6a467d1801fbb1a2983bf93f"
}

also, a Source metadata obj contains the entity key:


I need a new dbpopulater script, (make new Dbpopulator called 'DAta Correction') that will properly unify 2 different entities. 
It will read this csv:
final_display_name,display_names_to_join,merged
God,god|god hashem|god most high|god of abraham|hashem,


find the entitys with this display name, and combine them to one. there will now only be 1 obj in the db to represent this entity, with all relevent relationships, and Source metadata objs, updated to only point to that obj. 

in the event that we find more than 1 entity for that display_name, than clearly log it, and skip that line. 
after running the whole csv, the program should mark merged or not. 
if there was an error while running, the file should mark 'merged' for whatever it was successful with.



}}

fix entity rel population (pydantic){{
pydantic creating graph, remove entitys with display name like (.lower() (not contained, exactly equal..)

	in the prompt, give it the enums to guide it.. should be alphabetized !

	Person -all family relationship general words - get a huge list.
			son, sons, daughter, daughters, wife, wives, husband, husbands, -
			king
	Place - house, city, plain, hills, entrace, gate, door, Field,

}}

old prompts {{





Facets:
This should be a few sections, each one checkboxes/ staggered checkboxes.
section1 - Source Type - populated by backend.models_db.Enums.SourceType
section2 - Book - pop by backend.db.data_names.Books.Books. There must be sub sections, group by Book Category. ensure that you keep the order with the order field
section3- Passage Type - pop by backend.models_db.Enums.PassageType
section4- Entity - these entitys: Animal, Food, Nation, Number, Person, Place, Plant, Symbol, TribeOfIsrael. each should be their on sub section. there should be a button (label=select) at each section, which will open up a popup. in the popup, for now, leave it blank.


try to make these facets look nice, well layout, 

----

-----
in Facets:
 
 - select all / none etc is glitchy. 
 (1) i dont like the V and X anymore. instead, just put a checkbox at section header. 
 (2) init all the checkboxes to selected for all facets
 (3) the section header checkboxes can have three states - fully checked (select All), fully un checked (None), or partially checked (however you want to depict that). 
 (4) collapsing a section should not impact all any selections of that section.

 

	- now lets work on the entity selector componenet of the facet
	(0) each entity selection component should be the same one used in that individual Entity search page. frontend.pages.entity_search.render
	(1) remove the button of each entity facet section and any logic around it.
	(2) each entity section, when expanded, should have a dropdown select list. exactly like done in entity_Search.
	
	
	 - its so slow!
	 
	 
	
	
	[should do with sonnet, see, and then potentially do with Opus]
	- currently, this func backend.file_utils.HtmlWriter.HtmlWriter.get_full_html creates an html from the sources list. I want you to undertand how this html is built, and recreate it uses more native feeling streamlit components. See the general style of my ui (look at frontend/assets/facets.css and frontend/assets/styles.css etc) . feel free to add more files if needed. 
	


(dovid, test this page out a little)

-----
fix pop up to select entitys .
----
ensure Search page works in Hebrew (RTL) properly. When in RTL, the facets should appear on the right side.
----

----

implement the link to open source to see where entity came from
--
(after implementing the other ones, add the links to the other entitys..) also, it should count how many times each relationship is found in a source, and bring the most common ones to the top
---
write quality QA for source searching. also source populating the rel and entity (give the llm an example from the json and ask it to very it made those objects)


############################ OPUS ###################
----
look at the flow of retrieving all the SelectOptions. My SelectOptions list is meant to grow to be at least 10,000, maybe even 100,000 big. 
any way to speed up loading these terms? i dont want pagination, i want to load them all into that dropdown. maybe a way to speed it up with multi threading or anything like that?

also, can you add a spinner when it's waiting?
--
###############################################


UNTIL WE DONT FIX THE FE, NO POINT TO FIX THE LOGIC...
take a look at how the parameters come from the front end to the source search query logic. Something is missed, and the filtering doesnt work. look it over and tell me whats going on. 

-----
allow filtering the sources with the same left pane as there is in source search?

------






}}