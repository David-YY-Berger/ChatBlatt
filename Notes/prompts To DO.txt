
todo {{

to Number,Category add a bunch of things (gematria, etc)

populate BT:
	- keep getting lists of duplicate names, began with claude's.... also in gemara, also in tanach + gemara (Shmuel, rachel, hillel)
	- 
	Also, the "transaction" in test_populate_entities_and_rels_from_jsons doesn't actually roll anything back, because the session isn't passed to the DB calls. That was already the case before my change, so a crash still leaves partial writes.
	
	
	- work on prepopulate script, getting data into readable json, and ensuring spelling is exact btw claude + current db
	- insert_entity, make sure to check also display_en_name

write ton of easy prompts to commit while working...
	
search sources must be halak
hebrew working


}}

easy fixes {{
-------
put sefaria logo on same level as 'Mapali' title , to the right side (both mapali and sefaria logo should be above the subtile of 'the indexed database of Jewish Sources'
------------------
(do ctrl+f search first) 
in all places, both hebrew and enlgish, replace mapaLi with 'Eitz Habinah' עץ הבינה.
------------------
source search:
remove all the UI front end code on the left side with checkboxes, logic, etc with Passage type, and filtering by source. instead, on the left side, there should be
1. a simple, COMPACT section to select Passage Type (no 'all' or 'none' buttons), just those 5 checkboxes. remove the '0/5 selected' label, make this more slimly compact in width
2. a simple drop down, call 'Books'. This dropdown with look more or less like the entity dropdowns, but will filter by source's book instead.

Review the relevant backend searching code, ensure that this 2 filters work smoothly.
-------
safetly remove this class backend.db.EntityRelManager.EntityRelManager
------ 
add to backend.models_db.Enums.NumberCategory (make sure to update all usages!!! inlcuding C:\Users\U6072661\PycharmProjects\ChatBlatt\frontend\pages\number_search.py, backend, frontend, )
these categorys: Gematria (numeric value of a word), Obligation, Punishment

}}


fix up identifying ambigious entityes {{ 

in this backend_pipeline.data_pipeline.populator_scripts.DBPopulateEntityRelGraph.DBPopulateEntityRelGraph populator, i have this problem:
There can be 2 or more different people throughout my corpus (TN, BT) with the same exact display_en_name derived with the LLM. But I must register them differently. 
Before I say the algo how to solve this, tell me exactly what my current program does about this.

[pre populate -C:\Users\U6072661\PycharmProjects\ChatBlatt\backend_pipeline\data_pipeline\PydanticModels\ambigous_entities\Person: decide_entity


i'm giving you my desired algorithim for disambiguation. Note - in practice, before i actually run this population on any real sources, i plan to perform a pre population of a lot of different major people in Tanach and Talmud that are known and have same name. So for many major ambigous entities, there will be already 2+ existing entitys in the db.

Algo:
(remember, we are iterating over the entitys/relationships taken from a given source, and trying to correctly populate these to our exisiting db):
for entity (and this current source):

if 0 other exisiting entity with this entity_type and display_en_name in our db: create_new_entity
else :
	if 1 other entity with this entity_type and display_en_name in our db: 
		if in this current passage, our new entity has father, mother:
			if it contradicts the existing entity: create_new_entity
			else: add_to_existing_entity
		# assuming there is no ambigious entity with same father/mother as it's partner ambigious entity
		else: add_to_existing_entity # bec we have no clear indiciation that this new entity is different than the current. the other relationships of person are not unique. 
			
	else (2+ entitys with this entity_type and display_en_name): decide_entity
	
carefully lear garbage code or other things that will get in the way of this
implement this code clear and properly. do not yet implement 'decide_entity', but make that function in the correct place, i will soon tell you to implement that.
		
----------------

ADD CONTEXT: example json response, Books, Source Content, etc.

implement decide_entity. see this information list. note - at this point in the code, we have NO metadata at all for the current new entity we discovered, we only have: (1) relationships inside that passage (2) other entitys mentioned in that passage (3) that passage book, etc - we have SourceContent, but not Source Metadata. 

this is my algo. you can change or fix parts of it. It will likely necesitate lots of db calls; if there are simple ways for you to make this more efficient (maybe by caching some values?) , do it. but DO NOT CHANGE ANY DB SCHEMAS, DONT ADD ANYTHING TO THE DB, AND DONT BREAK ANYTHING ELSE. this pipeline is not time sensitive.


algo:
if this new entity has any relationships: iterate over exisiting entitys, and see which existing entity has the most 'common' relationships with this entity.
	if it's not a tie: add to entity with most number of common relationships
	else (it's a tie): prefer relationships of People, then Place, then any other relationship
if this new entity has no common relationships with either exisiting entity:
	cur_src_entities = get list of entities of this current source
	iterate over exisiting entitys, see which have most common related entities which appear in cur_src_entities.
		if it's not a tie: add to entity with most number of common 
		else: move on to next logic...
		
(could not determine based on related entitys nor other entities in this sources context):
	see current source's Book. if Tanach:
		map_exisiting_entitys_books = map of exisiting entitys to books that mention them
		if current source is only in one of map_exisiting_entitys_books: add this new entity to that entity

else { copilot, think of some last fallback logic}
	
		

}}

house
gate


old prompts {{





Facets:
This should be a few sections, each one checkboxes/ staggered checkboxes.
section1 - Source Type - populated by backend.models_db.Enums.SourceType
section2 - Book - pop by backend.db.data_names.Books.Books. There must be sub sections, group by Book Category. ensure that you keep the order with the order field
section3- Passage Type - pop by backend.models_db.Enums.PassageType
section4- Entity - these entitys: Animal, Food, Nation, Number, Person, Place, Plant, Symbol, TribeOfIsrael. each should be their on sub section. there should be a button (label=select) at each section, which will open up a popup. in the popup, for now, leave it blank.


try to make these facets look nice, well layout, 

----

-----
in Facets:
 
 - select all / none etc is glitchy. 
 (1) i dont like the V and X anymore. instead, just put a checkbox at section header. 
 (2) init all the checkboxes to selected for all facets
 (3) the section header checkboxes can have three states - fully checked (select All), fully un checked (None), or partially checked (however you want to depict that). 
 (4) collapsing a section should not impact all any selections of that section.

 

	- now lets work on the entity selector componenet of the facet
	(0) each entity selection component should be the same one used in that individual Entity search page. frontend.pages.entity_search.render
	(1) remove the button of each entity facet section and any logic around it.
	(2) each entity section, when expanded, should have a dropdown select list. exactly like done in entity_Search.
	
	
	 - its so slow!
	 
	 
	
	
	[should do with sonnet, see, and then potentially do with Opus]
	- currently, this func backend.file_utils.HtmlWriter.HtmlWriter.get_full_html creates an html from the sources list. I want you to undertand how this html is built, and recreate it uses more native feeling streamlit components. See the general style of my ui (look at frontend/assets/facets.css and frontend/assets/styles.css etc) . feel free to add more files if needed. 
	


(dovid, test this page out a little)

-----
fix pop up to select entitys .
----
ensure Search page works in Hebrew (RTL) properly. When in RTL, the facets should appear on the right side.
----

----

implement the link to open source to see where entity came from
--
(after implementing the other ones, add the links to the other entitys..) also, it should count how many times each relationship is found in a source, and bring the most common ones to the top
---
write quality QA for source searching. also source populating the rel and entity (give the llm an example from the json and ask it to very it made those objects)


############################ OPUS ###################
----
look at the flow of retrieving all the SelectOptions. My SelectOptions list is meant to grow to be at least 10,000, maybe even 100,000 big. 
any way to speed up loading these terms? i dont want pagination, i want to load them all into that dropdown. maybe a way to speed it up with multi threading or anything like that?

also, can you add a spinner when it's waiting?
--
###############################################


UNTIL WE DONT FIX THE FE, NO POINT TO FIX THE LOGIC...
take a look at how the parameters come from the front end to the source search query logic. Something is missed, and the filtering doesnt work. look it over and tell me whats going on. 

-----
allow filtering the sources with the same left pane as there is in source search?

------






}}