current issues:
- light mode coloring is off
- in parent tab of sub tabs - the fill coloring is black, not blue. fix it
- the coloring is off in the sub tabs as well and the whole pane which expands downward.
- the language radio button should also flip sides when language is changed. currently, on hebrew side it incorrectly stays on the right side.
- the language options should be veritcally stacked, not horizontal
- there is a strangly visible white line on the tab of the hotizontal tab panes. make sure its removed.

